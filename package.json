{
  "name": "mcp-lab",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b && node scripts/copy-cli.mjs",
    "dev": "tsx packages/cli/src/cli.ts",
    "clean": "rm -rf dist packages/*/dist",
    "format": "npx prettier --write \"packages/**/*.{ts,jsx,css}\" --config ./.prettierrc",
    "test": "echo 'test completed'"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
